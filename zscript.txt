version "4.3"

class VD_VersionHandler : StaticEventHandler
// Technically this just exists to throw the version number to the console.
{
	override void OnRegister()
	{
		string vdVersion = string.Format(StringTable.Localize("$VD_VERSION"));
		Console.Printf("%s%s",vdVersion);
	}
}
